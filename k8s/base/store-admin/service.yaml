apiVersion: v1
kind: Service
metadata:
  name: store-admin
  labels:
    app: store-admin
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 8081
      protocol: TCP
  selector:
    app: store-admin
